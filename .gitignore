# Build artifacts
dist/
tmail-mcp
tmail-mcp.exe

# npm
node_modules/
node_modules/.ignored/
*.tgz
pnpm-lock.yaml
bun.lock
bun.lockb
yarn.lock

# Generated OpenAPI types (dev-only, optional)
src/schemas/generated/

# Removed from package — dev-only or inlined
scripts/
openapi/
snippets/

# Local wallet profiles (never commit)
.tmail/

# Self-host deploy scripts (optional, not for npm)
deploy/

# Test temp
.tmail-test-*
gate-*/
